//! The parts of fastmemory MahaBodi uses (ATF parser and inline Louvain), vendored verbatim; see each file.
//! Not included: telemetry, server, MCP and CLI.
#![allow(dead_code, clippy::all)]
pub mod cluster;
pub mod parser;

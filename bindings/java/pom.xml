<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>io.github.mahabodi</groupId>
  <artifactId>mahabodi</artifactId>
  <version>0.1.0</version>
  <packaging>jar</packaging>
  <name>MahaBodi</name>
  <description>MahaBodi: a System-1 engine for AI agents (fastmemory topology memory + Laya decisions). JNI binding with bundled native libraries for linux-x86_64 and macos-x86_64.</description>
  <url>https://github.com/mahabodi/mahabodi</url>
  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
      <distribution>repo</distribution>
    </license>
  </licenses>
  <developers>
    <developer>
      <name>MahaBodi contributors</name>
      <url>https://github.com/mahabodi</url>
    </developer>
  </developers>
  <scm>
    <connection>scm:git:https://github.com/mahabodi/mahabodi.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/mahabodi/mahabodi.git</developerConnection>
    <url>https://github.com/mahabodi/mahabodi</url>
  </scm>
  <properties>
    <maven.compiler.release>11</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Tests use the locally built library: cargo build (release profile) -p mahabodi-jni -->
    <bodi.native>${project.basedir}/../../target/release/libmahabodi_jni.dylib</bodi.native>
  </properties>
  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <resources>
      <resource><directory>src/main/resources</directory></resource>
      <resource>
        <directory>${project.basedir}/../..</directory>
        <targetPath>META-INF</targetPath>
        <includes><include>LICENSE</include></includes>
      </resource>
      <!-- META-INF/NOTICE (third-party notices, incl. the vendored fastmemory code in the natives);
           written by scripts/java_natives.sh, not committed -->
      <resource>
        <directory>${project.basedir}/meta</directory>
        <targetPath>META-INF</targetPath>
      </resource>
      <!-- Native libraries bundled into the jar: natives/<os>-<arch>/libmahabodi_jni.{so,dylib}
           (filled by scripts/java_natives.sh from one clean commit, with natives/BUILD_INFO.txt; not committed) -->
      <resource>
        <directory>${project.basedir}/natives</directory>
        <targetPath>natives</targetPath>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <argLine>-Dmahabodi.library.path=${bodi.native}</argLine>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution><id>attach-sources</id><goals><goal>jar-no-fork</goal></goals></execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.10.1</version>
        <configuration><doclint>all,-missing</doclint></configuration>
        <executions>
          <execution><id>attach-javadocs</id><goals><goal>jar</goal></goals></execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>linux</id>
      <activation><os><family>unix</family><name>Linux</name></os></activation>
      <properties><bodi.native>${project.basedir}/../../target/release/libmahabodi_jni.so</bodi.native></properties>
    </profile>
    <profile>
      <id>windows</id>
      <activation><os><family>windows</family></os></activation>
      <properties><bodi.native>${project.basedir}/../../target/release/mahabodi_jni.dll</bodi.native></properties>
    </profile>
    <!-- Maven Central release: mvn -B -Prelease deploy (signs with GPG; uploads to Central with
         autoPublish=false, so the deployment is checked and published manually in the Central portal) -->
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.7</version>
            <configuration>
              <!-- UpperSpace <prabhat@fastbuilder.ai>; passphrase entered via pinentry at publish time, never stored -->
              <keyname>881DF784DC270E90</keyname>
            </configuration>
            <executions>
              <execution><id>sign-artifacts</id><phase>verify</phase><goals><goal>sign</goal></goals></execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>0.7.0</version>
            <extensions>true</extensions>
            <configuration>
              <publishingServerId>central</publishingServerId>
              <autoPublish>false</autoPublish>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
